@model IEnumerable<Unipam.TCC.DAL.Entity.Entrega>

@{
    ViewBag.Title = "Index";
}

<h2>Inicio</h2>

<p>
    @Html.ActionLink("Criar Novo", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Etapa.IdEtapa)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Projeto.NomeProjeto)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Data)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Etapa.IdEtapa)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Projeto.NomeProjeto)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Data)
        </td>
        <td>
            @Html.ActionLink("Editar", "Edit", new { id=item.IdEntrega }) |
            @Html.ActionLink("Detalhes", "Details", new { id=item.IdEntrega }) |
            @Html.ActionLink("Deletar", "Delete", new { id=item.IdEntrega })
        </td>
    </tr>
}

</table>
