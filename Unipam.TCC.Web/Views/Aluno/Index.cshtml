@model IEnumerable<Unipam.TCC.DAL.Entity.Aluno>

@{
    ViewBag.Title = "Index";
}

<h2>Inicio</h2>

<p>
    @Html.ActionLink("Criar Novo", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Curso.NomeCurso)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Pessoa.Nome)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Curso.NomeCurso)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Pessoa.Nome)
        </td>
        <td>
            @Html.ActionLink("Editar", "Edit", new { id=item.IdPessoa }) |
            @Html.ActionLink("Detalhes", "Details", new { id=item.IdPessoa }) |
            @Html.ActionLink("Deletar", "Delete", new { id=item.IdPessoa })
        </td>
    </tr>
}

</table>
